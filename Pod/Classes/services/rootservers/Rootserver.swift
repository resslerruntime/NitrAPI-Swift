import ObjectMapper

open class Rootserver: Service {
    
    
    public required init?(map: Map) {
        super.init(map: map)
    }
    
    
    open override func mapping(map: Map) {
        super.mapping(map: map)
    }
    
}
